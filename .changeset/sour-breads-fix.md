---
---

Test-only: the setup-wizard suite now resolves the config directory per
platform instead of hardcoding Linux's. No user-facing change, so no version
bump — deliberately empty rather than a `patch`, which would have bumped
nanoterm past a `1.0.0` that has not been published yet.
